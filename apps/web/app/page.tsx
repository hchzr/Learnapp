import { PageShell } from "@/components/page-shell";

export default function HomePage() {
  return (
    <PageShell
      home
      title="Life & Learn"
      description="Shared UI foundations are now in place for typography, spacing, cards, loading states, and alerts."
    />
  );
}
