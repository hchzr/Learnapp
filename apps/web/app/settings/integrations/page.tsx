import { PageShell } from "@/components/page-shell";

export default function IntegrationsPage() {
  return (
    <PageShell
      title="Integrations"
      description="Placeholder for integration management (Notion, Todoist, Habitica, Google, and Anki)."
    />
  );
}
