# Docker Infrastructure Notes

Docker Compose configuration currently lives at repository root (`docker-compose.yml`) and provisions:
- PostgreSQL
- Redis

Future PRs may add service-specific Dockerfiles under this folder.
